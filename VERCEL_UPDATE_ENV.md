# Обновление переменных окружения на Vercel

## Проблема
Ваш опубликованный сайт (mgsouk.com) использует **старую базу данных Supabase**, а окно предпросмотра bolt.new использует **новую базу данных**.

Это видно по данным:
- **bolt.new**: 1 клиент, $0.00 баланс (новая база)
- **mgsouk.com**: 40 клиентов, $1,363,340.95 баланс (старая база)

## Решение: Обновить переменные окружения

---

## Шаг 1: Войдите в Vercel Dashboard

1. Откройте [vercel.com](https://vercel.com)
2. Нажмите **Log in** (если еще не вошли)
3. Выберите ваш проект из списка

**Внешний вид:**
- Вы увидите список всех ваших проектов
- Найдите проект, который связан с доменом `mgsouk.com`

---

## Шаг 2: Откройте настройки проекта

1. На странице проекта нажмите вкладку **Settings** (в верхнем меню)
2. В левом боковом меню найдите **Environment Variables**
3. Нажмите на **Environment Variables**

**Путь:** Project → Settings → Environment Variables

---

## Шаг 3: Найдите старые переменные

В списке переменных окружения найдите:
- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`

**Как они выглядят:**
- Имя переменной отображается слева
- Значение скрыто (показано как `•••••••••`)
- Справа есть кнопки действий (Edit, Delete)

---

## Шаг 4: Удалите старые переменные

Для каждой переменной (`VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY`):

1. Нажмите на **три точки** (⋯) справа от переменной
2. Выберите **Delete**
3. Подтвердите удаление

**Важно:** Удалите обе переменные, прежде чем добавлять новые.

---

## Шаг 5: Добавьте новые переменные

### 5.1 Добавить VITE_SUPABASE_URL

1. Нажмите кнопку **Add New** (в правом верхнем углу)
2. В поле **Key** введите: `VITE_SUPABASE_URL`
3. В поле **Value** введите:
   ```
   https://foyjaexevilxkbihwgcl.supabase.co
   ```
4. В разделе **Environments** выберите:
   - ✅ Production
   - ✅ Preview
   - ✅ Development
5. Нажмите **Save**

### 5.2 Добавить VITE_SUPABASE_ANON_KEY

1. Снова нажмите **Add New**
2. В поле **Key** введите: `VITE_SUPABASE_ANON_KEY`
3. В поле **Value** введите:
   ```
   eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImZveWphZXhldmlseGtiaWh3Z2NsIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjkwMjE5NzgsImV4cCI6MjA4NDU5Nzk3OH0.ET_Zl6qH5OvM5Sne7t_M5GFGnNnIg40z1moPwZjmrrI
   ```
4. В разделе **Environments** выберите:
   - ✅ Production
   - ✅ Preview
   - ✅ Development
5. Нажмите **Save**

---

## Шаг 6: Переразверните проект

После добавления новых переменных окружения нужно переразвернуть проект:

### Вариант 1: Через вкладку Deployments

1. Нажмите на вкладку **Deployments** (в верхнем меню)
2. Найдите последний успешный деплой (Latest Deployment)
3. Нажмите на **три точки** (⋯) справа
4. Выберите **Redeploy**
5. В модальном окне нажмите **Redeploy** для подтверждения

### Вариант 2: Через Git

Альтернативно, сделайте любой коммит в ваш Git репозиторий:
```bash
git commit --allow-empty -m "Redeploy with new environment variables"
git push
```

---

## Шаг 7: Проверьте результат

1. Дождитесь окончания деплоя (статус: **Ready**)
2. Откройте ваш сайт: [mgsouk.com/admin](https://mgsouk.com/admin)
3. Войдите как админ
4. Проверьте список клиентов

**Ожидаемый результат:**
- Должен быть 1 клиент (komisarenko84@bk.ru)
- Total Balance: $0.00
- Старые 40 клиентов больше не видны

---

## Проверка переменных

Чтобы убедиться, что переменные установлены правильно:

1. В Vercel перейдите: **Settings → Environment Variables**
2. Проверьте, что обе переменные присутствуют:
   - ✅ `VITE_SUPABASE_URL`
   - ✅ `VITE_SUPABASE_ANON_KEY`
3. Для каждой переменной должны быть отмечены все три окружения:
   - Production, Preview, Development

---

## Важные замечания

### ⚠️ Старая база данных
Данные из старой базы (40 клиентов, $1.3M баланс) **останутся в старой Supabase**, но сайт больше не будет к ней подключаться.

Если вам нужны эти данные, их следует экспортировать **до** обновления переменных окружения.

### ⚠️ Два админа
- **Старая база:** admin@mkmail.com
- **Новая база:** admin@mkmail.com (но с новым паролем)

После переключения вам нужно будет использовать **новые учетные данные админа**.

### ⚠️ Время применения
После сохранения переменных и редеплоя изменения вступят в силу **через 1-2 минуты**.

---

## Если что-то пошло не так

### Сайт не работает после обновления

1. Проверьте логи деплоя в Vercel
2. Убедитесь, что переменные скопированы **точно** (без лишних пробелов)
3. Попробуйте очистить кеш браузера: Ctrl+Shift+R (Windows) или Cmd+Shift+R (Mac)

### Старые данные все еще видны

1. Проверьте, что переменные действительно обновлены в Production окружении
2. Сделайте Hard Refresh страницы
3. Проверьте в DevTools → Network → проверьте запросы к Supabase (должны идти на `foyjaexevilxkbihwgcl.supabase.co`)

---

## Итоговый чеклист

- [ ] Зашел в Vercel Dashboard
- [ ] Открыл Settings → Environment Variables
- [ ] Удалил старые `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY`
- [ ] Добавил новую `VITE_SUPABASE_URL`
- [ ] Добавил новую `VITE_SUPABASE_ANON_KEY`
- [ ] Выбрал все три окружения для обеих переменных
- [ ] Сделал Redeploy проекта
- [ ] Дождался статуса Ready
- [ ] Открыл сайт и проверил данные
- [ ] Вижу 1 клиента вместо 40

---

## Контакты для поддержки

Если возникли проблемы:
1. Проверьте файл `.env` в локальном проекте - там правильные значения
2. Сравните значения в Vercel с локальными
3. Убедитесь, что деплой завершился успешно (зеленый статус)

---

**Успешного обновления! 🚀**
