# 🔄 Database Keep-Alive System

## Проблема
Supabase Free Tier автоматически **приостанавливает базы данных** после **7 дней неактивности**.

## ✅ Решение (3 уровня защиты)

### 1️⃣ Автоматический Cron (внутри БД)
База данных автоматически пингует себя **каждые 5 дней** через `pg_cron`.

**Статус:** ✅ Активно
**Проверка:**
```sql
SELECT jobname, schedule, active
FROM cron.job
WHERE jobname = 'keep-database-active';
```

### 2️⃣ Edge Function
Endpoint для мониторинга: `https://dxvicpfmxicjssciubvi.supabase.co/functions/v1/keep-alive`

**Тестирование:**
```bash
curl https://dxvicpfmxicjssciubvi.supabase.co/functions/v1/keep-alive
```

### 3️⃣ Веб-мониторинг (ручной)
Откройте: `https://ваш-сайт.com/keep-alive.html`

**Функции:**
- Автоматический пинг каждые 5 дней
- Ручной пинг кнопкой
- Лог активности
- Countdown до следующего пинга

---

## 📊 Таблица активности

В базе создана таблица `activity_log`:
- Хранит все пинги
- Автоматически удаляет записи старше 30 дней
- Доступна только админам

**Просмотр логов:**
```sql
SELECT * FROM activity_log
ORDER BY created_at DESC
LIMIT 10;
```

---

## 🔧 Дополнительные опции

### Вариант 1: UptimeRobot (бесплатно)
1. Зарегистрируйтесь на https://uptimerobot.com
2. Добавьте монитор:
   - Type: HTTP(s)
   - URL: `https://dxvicpfmxicjssciubvi.supabase.co/functions/v1/keep-alive`
   - Interval: 5 дней (или чаще)

### Вариант 2: Cron-job.org (бесплатно)
1. Зарегистрируйтесь на https://cron-job.org
2. Создайте задание:
   - URL: `https://dxvicpfmxicjssciubvi.supabase.co/functions/v1/keep-alive`
   - Every: 5 days

### Вариант 3: Supabase Pro Plan
- $25/месяц
- **Без автоматической паузы**
- Ежедневные бэкапы
- Point-in-time восстановление

---

## ⚠️ Важно

**Даже с keep-alive системой:**
- Делайте регулярные бэкапы (`pg_dump`)
- Храните важные данные в репозитории (миграции)
- Рассмотрите Pro Plan для продакшена

---

## 🎯 Текущий статус

✅ Cron задача активна (каждые 5 дней)
✅ Edge Function развернута
✅ Веб-мониторинг доступен
✅ Activity log работает

**База больше не будет останавливаться!** 🎉
